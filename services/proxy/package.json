{
  "name": "lithium-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Lithium Proxy — Cloudflare Worker for outbound web proxying",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
