[package]
name = "lithium-compute"
version = "0.1.0"
edition = "2021"
description = "Shared computation library for the Lithium server and WASM core"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
