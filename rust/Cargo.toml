[workspace]
members = [
    "lithium-abi",
    "filesystem", "snapshot_codec", "lz4", "xxh3",
    "lithium-compute",
    "server",
]
resolver = "2"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
codegen-units = 1
strip = true
