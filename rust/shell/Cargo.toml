[package]
name = "shell"
version = "0.1.0"
edition = "2021"
description = "Lithium shell WASM: notify, snap, lock, device, storage, memory, api, kv, tar"

[lib]
crate-type = ["cdylib"]

[dependencies]
lithium-abi = { path = "../lithium-abi" }
